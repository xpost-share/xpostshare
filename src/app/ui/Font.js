import {Manuale, Poppins} from "next/font/google";

export const nuale = Manuale({
    weight: '400',
    subsets: ["latin"],
})

export const poppins = Poppins({
    weight: '400',
    subsets: ["latin"],
})